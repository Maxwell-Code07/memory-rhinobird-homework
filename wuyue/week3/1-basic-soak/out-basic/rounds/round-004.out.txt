  ⚠ tirith security scanner enabled but not available — command scanning will use pattern matching only
大版本灰度发布，重点风险点我按"先验证链条、再谈细节"的思路给你拆——这个顺序跟你代码评审的习惯一致。

先说结论：灰度最怕的不是功能 bug，是**放量收不住**和**探测不到**。下周三发布，距现在只有正常工作日几天，你的检查应聚焦"能快速验证、可回滚、可观测"三类硬门槛。

一、放量控制与熔断（最优先）
- 灰度比例/分流是否真实生效？别只看配置写了 5%，用一流量/压测确认切过去的是预期比例。
- 是否有一键熔断/回滚通道？出事到全量回滚最多容忍几分钟——链路是否演练过，不是文档里有。
- 规则引擎/动态开关是否在极端流量下仍有性能余量？分级放量每一级的**观察窗口和判据**是什么，写死了没。

二、链路真实跑通（你的三门槛里最硬的一条）
- 新版本在灰度集群上，从网关→服务→存储的完整链路是否真的通了？不能只停在"服务起来了"或配置层。
- 可观测是否覆盖灰度分流本身——流量、错误率、延迟的对比维度 (gray vs stable) 是否打点真实，而不是同一条链路人肉比对?
- 探活/健康检查是否包含业务自检（依赖的 DB、Redis、消息队列），还是只测了进程活着。

三、数据与一致性（你在攻坚的分库分表/全局一致性直接相关）
- 灰度期间新旧版本是否可能并发写同一批数据？跨库的一致性问题，灰度会不会放大暴露?
- 数据迁移/格式变更步骤是否向前向后兼容，老版本读新数据有没有崩的风险。
- 灰度用户写入的数据，若回滚，脏写/重复是否可控、有无补偿。

四、变更面与依赖
- 灰度版本里"配置变更 / 依赖变更 / DB 变更"这三类，哪些是**不可以按比例拆**的（如 schema 变更、连接池、中间件），这些才是真正的炸弹——比例放量救不了它们。
- 依赖的中间件/第三方版本是否同步演进，有没有对外的契约断裂风险。

五、可回滚与发布协同（周三是大版本，通常牵多方）
- 若前端/App/其他团队同步发版，灰度能否按**他们**的节奏配比？跨团队回滚的边界协议是否预先对齐——最怕你回滚了，别人没回滚。
- 发布窗口内放量上限是谁拍板、什么条件下暂停全场放量。有没有一个明确的"红线人"。

再补一条流程层的：周三是大版本，肯定不是只有灰度，建议你牵头做一次**发布前 check 单 + 一次预演（canary 演练）**，把上面回滚/熔断/真实链路都按"出事就按键"真跑一遍，别只在方案上推演。你自己判据里"链路须真实跑通，非停在配置层"——这条同样适用于发布预案本身。

---
关于可观测选型（OTel vs Prometheus）你说的"结论待披露"我这边记着，如果你周三前想让我帮忙把某一项做成可执行的 check 清单或演练脚本，直接说范围，我能按你的三门槛框定来拆。

要不要我先把这份风险点整理成一个可勾选的发布检查单文件（按优先级排序、带判据和负责人列），你周三直接拿去用？
