  ⚠ tirith security scanner enabled but not available — command scanning will use pattern matching only
我们俩其实挺像的——你先把可观测选型卡在"链路真跑通"上,我接手一个编程任务,第一步锁定的也是同一件事:**先把边界和真实现状摸清,别急着动手**。拆开了大概是这么几步。

一、先读上下文而不是先写码
- 看有没有可复用的 skill(你嘴上说的"摸现成方案"),避免重新发明流程。
- 读目标代码/仓库结构,确认接口、约定、历史包袱——不是从零猜。
- 用户有没有明确的验收标准;没有就先拿低风险歧义按默认判,需要定夺的拆出来问。

二、把任务拆成清单再动手
- 一个 3+ 步或涉及多文件的活儿,先写 todo 列表,每项是可验证的完成标准,不是空话。
- 这样能并行、能断点续传、能知道你卡在哪个子问题上。

三、真实执行优先于描述
- 说"我会跑测试"就真跑;说"我建了项目"就真建。收尾一定拿出真实工具输出(测试通过、进程起来、文件写到),补一个能核实的证据(绝对路径、URL、退出码),绝不用听起来像真的假结果糊弄。

四、按次序验收
- 你说过评审要"先单测覆盖、再实现细节"——这条我当成普适纪律用:先确证被改了的东西真被覆盖住、链路真跑通,再谈怎么实现更漂亮。
- 交差 = 每一条验收标准都被验证过,不是"实现了我计划里的东西"。

五、顺手沉淀
- 每次趟出非平凡的工作流,存进 skill,下次直接复用;你的每个灰度和评审节点也一样,是可以迁移进团队流程规范的样本。

所以给到你的一句话回答:**我先做证据采集和边界确认(读上下文、找可复用资产、拆验收清单),确认"链路能被真跑通、覆盖能被看到"之后,才允许自己动第一行代码。**

这套东西和你对付灰度发布用的是同一个引擎——我先有把握,才落地。
